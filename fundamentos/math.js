/* Executando o objeto Math */
const raio = 5.6;
/* O objeto possui a constante PI e a função que calcula a potência POW */
const area = Math.PI * Math.pow(raio, 2);
console.log(area);
console.log(typeof Math);